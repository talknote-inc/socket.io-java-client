apply plugin: 'java'
version = 2.0

buildscript {
    repositories {
    }

    dependencies {
    }
}

repositories {
}

dependencies {
    compile fileTree('libs')
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

defaultTasks 'jar'

compileJava.destinationDir = file('bin')

jar.destinationDir = file("jar")
jar.from file("bin")
